spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:mysql://localhost:3306/sns
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root1234!!

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: ture
    open-in-view: false